import 'package:flutter_test/flutter_test.dart';

void main() {
  test('App compiles and runs', () {
    // TODO: Thêm mock AuthController + ParentHomeController để test UI
    expect(1 + 1, equals(2));
  });
}
